version: '3.4'

services:
  moodtrackingapplication:
    image: moodtrackingapplication
    build:
      context: .
      dockerfile: ./Dockerfile
    secrets:
      - password_secret

secrets:
  password_secret:
    file: ./password.txt
